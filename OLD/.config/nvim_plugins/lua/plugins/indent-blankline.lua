require("indent_blankline").setup {
    -- for example, context is off by default, use this to turn it on
    -- show_current_context = true, -- Highlight current scope
    -- show_current_context_start = true, -- Highlight current scope's start (bad)
}
